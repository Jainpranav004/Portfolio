/** Canonical site origin (no trailing slash). Used for OG URLs, canonicals, JSON-LD. */
export const SITE_URL = "";

/** Shown in the UI and used for mailto: links. */
export const CONTACT_EMAIL = "Jainapranav02@gmail.com";
export const DISPLAY_EMAIL = CONTACT_EMAIL;
